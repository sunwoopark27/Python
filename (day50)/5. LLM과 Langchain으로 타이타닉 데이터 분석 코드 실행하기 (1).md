
파일 경로 : condatest > day_50 > titanictest.py
## 실습 목표

Langchain과 LLM을 결합해서 타이타닉 데이터셋을 분석하는 파이썬 코드를 생성하고, 이를 Langchain을 활용해 실행하는 방법을 학습합니다.

## 실습 목차

1. **데이터 특징 추출:** 타이타닉 데이터셋이 어떻게 구성되어 있는지 추출하고 이를 시스템 프롬프트에 적용합니다.
2. **데이터 분석 체인 구성:** 데이터를 분석하는 코드를 생성 및 실행하고, 그 결과를 출력하는 체인을 구성합니다.

## 실습 개요

LangChain을 활용해서 자연어로 타이타닉 데이터를 분석할 수 있는 챗봇을 구현하고 사용해봅니다.

## 0. 환경 설정

필요한 라이브러리를 불러옵니다.

```python
import contextlib
import io
import os

import pandas as pd
from langchain_community.chat_models import ChatOllama
from langchain_community.embeddings import OllamaEmbeddings
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.runnables import RunnablePassthrough
from langchain_experimental.tools.python.tool import PythonAstREPLTool

```

Ollama를 통해 Mistral 7B 모델을 불러옵니다. 처음 불러오는데는 다운로드 시간 포함 약 3분이 소요됩니다.

```python
!ollama pull mistral:7b

```

## 1. 데이터 특징 추출

타이타닉 데이터셋이 어떻게 구성되어 있는지 추출하고 이를 시스템 프롬프트에 적용합니다.

먼저, mistral:7b 모델을 사용하는 ChatOllama 객체와 OllamaEmbeddings 객체를 생성합니다.

```python
llm = ChatOllama(model="mistral:7b")
embeddings = OllamaEmbeddings(model="mistral:7b")

```

다음으로, 타이타닉 데이터를 불러오고, 데이터의 컬럼명을 변수에 저장합니다.

```python
# 데이터를 불러오고, 이름과 컬럼명을 저장합니다.
# 실습용 데이터로 타이타닉 데이터셋을 사용합니다
from sklearn.datasets import fetch_openml
titanic = fetch_openml(name='titanic', version=1, as_frame=True)
df_titanic = titanic.data
df_titanic['survived'] = titanic.target

# 데이터를 저장한 변수명을 LLM에 제공하여 이 변수를 활용하는 코드를 작성하게 할 수 있습니다.
df_name = "df_titanic"
df_columns = ", ".join(df_titanic.columns)

```

다음으로, 데이터의 컬럼명을 바탕으로 코드를 쿼리하는 프롬프트를 작성합니다.

```python
system_message = "당신은 타이타닉 데이터를 분석하는 데이터 분석가입니다.\\n"
system_message += f"주어진 DataFrame에서 데이터를 출력하여 주어진 질문에 답할 수 있는 파이썬 코드를 작성하세요. {df_name} DataFrame에는 액세스할 수 있습니다.\\n"
system_message += f"`{df_name}` DataFrame에는 다음과 같은 열이 있습니다: {df_columns}\\n"
system_message += "각 열의 의미:\\n"
system_message += "- pclass: 승객 등급 (1 = 1등석, 2 = 2등석, 3 = 3등석)\\n"
system_message += "- sex: 성별 (male/female)\\n"
system_message += "- age: 나이\\n"
system_message += "- sibsp: 함께 탑승한 형제자매 및 배우자의 수\\n"
system_message += "- parch: 함께 탑승한 부모 및 자녀의 수\\n"
system_message += "- fare: 운임 요금\\n"
system_message += "- embarked: 승선한 항구 (C = Cherbourg, Q = Queenstown, S = Southampton)\\n"
system_message += "- boat: 탈출한 구명보트 번호\\n"
system_message += "- body: 시신 식별 번호\\n"
system_message += "- home.dest: 집/목적지\\n"
system_message += "- survived: 생존 여부 (1 = 생존, 0 = 사망)\\n"
system_message += "데이터는 이미 로드되어 있으므로 데이터 로드 코드를 생략해야 합니다."

message_with_data_info = [
    ("system", system_message),
    ("human", "{question}"),
]

```

시스템 프롬프트를 확인해 봅시다.

```python
print(system_message)

```

이전 실습에서 구성한 체인과 비슷하게, 코드를 생성하는 체인을 구성하고 실행해 봅시다.

```python
prompt_with_data_info = ChatPromptTemplate.from_messages(message_with_data_info)

# 체인 구성
code_gen_chain = ####

print(code_gen_chain.invoke("나이가 가장 많은 승객은 누구인가요?"))

```

## 2. 데이터 분석 체인 구성

데이터를 분석하는 코드를 생성하고, 코드를 실행하고, 그 결과를 답변하는 체인을 구성합니다.

### 2-1. 코드 복사

먼저, LLM이 생성한 코드를 찾아서 복사하는 기능을 추가해 봅시다.

```python
def python_code_parser(input: str) -> str:
    # LLM은 대부분 ``` 블럭 안에 코드를 출력합니다. 이를 활용합니다.
    # ```python (코드) ```, 혹은 ``` (코드) ``` 형태로 출력됩니다. 두 경우 모두에 대응하도록 코드를 작성합니다.
    processed_input = input.replace("```python", "```").strip()
    parsed_input_list = processed_input.split("```")

    # 만약 ``` 블럭이 없다면, 입력 텍스트 전체가 코드라고 간주합니다.
    if len(parsed_input_list) == 1:
        return processed_input

    # 코드 부분만 추출합니다.
    # LLM은 여러 코드 블럭에 걸쳐 필요한 코드를 출력할 수 있으므로, 코드가 있는 홀수 번째 텍스트를 모두 저장합니다.
    parsed_code_list = []
    for i in range(1, len(parsed_input_list), 2):
        parsed_code_list.append(parsed_input_list[i])

    # 코드 부분을 하나로 합칩니다.
    return "\\n".join(parsed_code_list)

code_gen_chain_with_parser = (
    code_gen_chain
    | python_code_parser
)

print(code_gen_chain_with_parser.invoke("나이가 가장 많은 승객은 누구인가요?"))

```

---

여기까지 푸시면 됩니다

### 2-2. 코드 실행

코드만 깔끔하게 출력했으니, 이제 이 코드를 실행해봅시다.

```python
def run_code(input_code: str):
    # 코드가 출력한 값을 캡쳐하기 위한 StringIO 객체를 생성합니다.
    output = io.StringIO()
    try:
        # Redirect stdout to the StringIO object
        with contextlib.redirect_stdout(output):
            # 코드가 실행하면서 출력한 모든 결과를 캡쳐합니다.
            exec(input_code, {"df_titanic": df_titanic})
    except Exception as e:
        # 에러가 발생할 경우, 이를 StringIO 객체에 저장합니다.
        print(f"Error: {e}", file=output)
    # StringIO 객체에 저장된 값을 반환합니다.
    return output.getvalue()

```

```python
code_execute_chain = ####

print(code_execute_chain.invoke("나이가 가장 많은 승객은 누구인가요?"))
```

만약 위와 같이 `stdout`을 캡쳐하는 것이 불가능하다면, Langchain-experimental의 `PythonAstREPLTool`을 활용할 수도 있습니다.

```python
repl_execute_chain = (
    code_gen_chain_with_parser |
    PythonAstREPLTool(globals={"df_titanic": df_titanic})
)

print(repl_execute_chain.invoke("나이가 가장 많은 승객은 누구인가요?"))

```

### 결과 설명 챗봇 구현

`code_execute_chain` 체인과 다른 구성 요소를 활용해서 단순 데이터가 아닌 분석 결과를 설명해주는 챗봇을 구현해보세요.

```python
# 데이터 분석 결과를 받아서 사용자 질문과 함께 LLM에 전달하는 체인
analysis_prompt = ChatPromptTemplate.from_messages([
    ("system", "당신은 타이타닉 데이터 분석 결과를 해석하고 설명하는 전문가입니다. 제공된 데이터 분석 결과를 바탕으로 사용자의 질문에 명확하게 답변해주세요."),
    ("human", "사용자 질문: {question}\\n\\n데이터 분석 결과: {analysis_result}")
])

explain_chain = (
    {"question": lambda x: x, "analysis_result": code_execute_chain}
    | analysis_prompt
    | llm
    | StrOutputParser()
)

# 설명 체인을 실행해 봅시다
print(explain_chain.invoke("남성과 여성의 생존율을 비교해서 설명해주세요"))

```

## 퀴즈

1. LLM과 Langchain을 이용한 데이터 분석에서 시스템 프롬프트에 데이터의 컬럼 정보를 제공하는 이유는 무엇인가요?
2. 위 코드에서 `python_code_parser` 함수의 역할은 무엇인가요?
3. 원본 코드의 마지막 부분에서 발생했던 문제점(단순 데이터만 반환하는 문제)을 어떻게 해결했나요?
4. 직접 다음 질문에 대한 답을 얻는 코드를 작성해보세요: "타이타닉에서 1등석, 2등석, 3등석 승객들의 평균 생존율은 어떻게 되나요?"
5. 위에서 구현한 `explain_chain`을 개선하여 코드와 데이터 분석 결과를 같이 보여주는 체인을 구현해보세요.